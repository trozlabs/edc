const { Type, Text } = require('./statics');
const { objectQuery, objectRemap, fsTree } = require('./functions');

module.exports = {
    Type,
    Text,

    objectQuery,
    objectRemap,
    fsTree,
}